
/workspaces/paeslab/src/app/aprender/[testType]/[unitSlug]/page.tsx
  17:21  error  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

/workspaces/paeslab/src/app/aprender/[testType]/[unitSlug]/page.tsx:17:21
  15 |  async function load(){const testType=String(params.testType??'m1').toUpperCase();const unitSlug=String(params.unitSlug??'');const {data:unitData}=await supabase.from('learning_units').select('*').eq('test_type',testType).eq('slug',unitSlug).eq('is_published',true).maybeSingle();if(!unitData){setLoading(false);return;}setUnit(unitData as LearningUnit);const {data:lessonData}=await supabase.from('lessons').select('*').eq('unit_id',unitData.id).eq('is_published',true).order('sort_order');setLessons((lessonData??[]) as Lesson[]);const {data:{user}}=await supabase.auth.getUser();if(user){const {data:p}=await supabase.from('lesson_progress').select('lesson_id,status,progress_percent,best_quiz_score,last_block_order').eq('user_id',user.id);setProgress((p??[]) as LessonProgress[]);}setLoading(false);}
  16 |
> 17 | useEffect(()=>{void load();},[params]);
     |                     ^^^^ Avoid calling setState() directly within an effect
  18 |
  19 |  if(loading)return <main className="grid min-h-screen place-items-center bg-slate-950 text-white">Cargando unidad...</main>;
  20 |  if(!unit)return <main className="grid min-h-screen place-items-center bg-slate-950 text-white"><div className="text-center"><h1 className="text-3xl font-black">Unidad no encontrada</h1><Link href="/aprender" className="mt-5 inline-block text-teal-300">Volver</Link></div></main>;  react-hooks/set-state-in-effect

/workspaces/paeslab/src/app/aprender/leccion/[slug]/page.tsx
  180:10  error  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

/workspaces/paeslab/src/app/aprender/leccion/[slug]/page.tsx:180:10
  178 |
  179 | useEffect(() => {
> 180 |     void load();
      |          ^^^^ Avoid calling setState() directly within an effect
  181 |   }, [params]);
  182 |
  183 |  react-hooks/set-state-in-effect

/workspaces/paeslab/src/app/aprender/page.tsx
  32:21  error  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

/workspaces/paeslab/src/app/aprender/page.tsx:32:21
  30 |   }
  31 |
> 32 | useEffect(()=>{void load();},[]);
     |                     ^^^^ Avoid calling setState() directly within an effect
  33 |
  34 |
  35 |   const m1=units.filter(unit=>unit.test_type==='M1');  react-hooks/set-state-in-effect

/workspaces/paeslab/src/app/dashboard/page.tsx
  237:10  error  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

/workspaces/paeslab/src/app/dashboard/page.tsx:237:10
  235 |
  236 | useEffect(() => {
> 237 |     void loadDashboard();
      |          ^^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  238 |   }, []);
  239 |
  240 |  react-hooks/set-state-in-effect

/workspaces/paeslab/src/app/historial/page.tsx
  168:10  error  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

/workspaces/paeslab/src/app/historial/page.tsx:168:10
  166 |
  167 | useEffect(() => {
> 168 |     void loadHistory();
      |          ^^^^^^^^^^^ Avoid calling setState() directly within an effect
  169 |   }, []);
  170 |
  171 |  react-hooks/set-state-in-effect

/workspaces/paeslab/src/app/mi-plan/page.tsx
  104:11  error  Error: Cannot access variable before it is declared

`refreshRecommendations` is accessed before it is declared, which prevents the earlier access from updating when this value changes over time.

/workspaces/paeslab/src/app/mi-plan/page.tsx:104:11
  102 |
  103 |     setUserId(user.id);
> 104 |     await refreshRecommendations(false);
      |           ^^^^^^^^^^^^^^^^^^^^^^ `refreshRecommendations` accessed before it is declared
  105 |     setLoading(false);
  106 |   }
  107 |

/workspaces/paeslab/src/app/mi-plan/page.tsx:113:3
  111 |
  112 |
> 113 |   async function refreshRecommendations(showResult = true) {
      |   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
> 114 |     setRefreshing(true);
      | ^^^^^^^^^^^^^^^^^^^^^^^^
> 115 |     setMessage("");
      …
      | ^^^^^^^^^^^^^^^^^^^^^^^^
> 138 |     setRefreshing(false);
      | ^^^^^^^^^^^^^^^^^^^^^^^^
> 139 |   }
      | ^^^^ `refreshRecommendations` is declared here
  140 |
  141 |   async function loadItems() {
  142 |     const { data, error } = await supabase  react-hooks/immutability
  109:10  error  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

/workspaces/paeslab/src/app/mi-plan/page.tsx:109:10
  107 |
  108 | useEffect(() => {
> 109 |     void initialize();
      |          ^^^^^^^^^^ Avoid calling setState() directly within an effect
  110 |   }, []);
  111 |
  112 |                                                                                                                                            react-hooks/set-state-in-effect

/workspaces/paeslab/src/app/practicar-leccion/[lessonId]/page.tsx
  215:10  error  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

/workspaces/paeslab/src/app/practicar-leccion/[lessonId]/page.tsx:215:10
  213 |
  214 | useEffect(() => {
> 215 |     void initialize();
      |          ^^^^^^^^^^ Avoid calling setState() directly within an effect
  216 |   }, [params]);
  217 |
  218 |  react-hooks/set-state-in-effect

/workspaces/paeslab/src/app/ruta-estudio/page.tsx
  18:21  error  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

/workspaces/paeslab/src/app/ruta-estudio/page.tsx:18:21
  16 |  async function load(){const {data:{user}}=await supabase.auth.getUser();if(!user){router.replace('/login');return;}setUserId(user.id);const [{data:attemptData},{data:unitData},{data:lessonData},{data:progressData},{data:planData}]=await Promise.all([supabase.from('question_attempts').select('is_correct,questions(axis)').eq('user_id',user.id).order('created_at',{ascending:false}).limit(300),supabase.from('learning_units').select('*').eq('test_type','M1').eq('is_published',true),supabase.from('lessons').select('*').eq('is_published',true),supabase.from('lesson_progress').select('lesson_id,status').eq('user_id',user.id),supabase.from('study_plan_items').select('lesson_id').eq('user_id',user.id).in('status',['pending','in_progress'])]);const units=(unitData??[]) as LearningUnit[];const lessons=(lessonData??[]) as Lesson[];const completed=new Set((progressData??[]).filter(p=>p.status==='completed').map(p=>p.lesson_id));setPlan((planData??[]).map(p=>p.lesson_id));const map=new Map<string,{total:number;correct:number}>();for(const attempt of (attemptData??[]) as Attempt[]){const q=Array.isArray(attempt.questions)?attempt.questions[0]:attempt.questions;if(!q?.axis)continue;const current=map.get(q.axis)??{total:0,correct:0};current.total++;if(attempt.is_correct)current.correct++;map.set(q.axis,current);}const ranked=units.map(unit=>{const v=map.get(unit.axis);return {unit,accuracy:v?Math.round(v.correct/v.total*100):null,total:v?.total??0};}).sort((a,b)=>(a.accuracy??-1)-(b.accuracy??-1));const result:Recommendation[]=[];for(const entry of ranked){const candidates=lessons.filter(l=>l.unit_id===entry.unit.id&&!completed.has(l.id));for(const l of candidates.slice(0,2)){result.push({...l,axis:entry.unit.axis,unitSlug:entry.unit.slug,accuracy:entry.accuracy,reason:entry.accuracy===null?'Aún no hay suficientes respuestas en este eje.':`Tu precisión reciente en ${entry.unit.axis} es ${entry.accuracy}%.`});}if(result.length>=6)break;}setRecommendations(result.slice(0,6));setLoading(false);}
  17 |
> 18 | useEffect(()=>{void load();},[]);
     |                     ^^^^ Avoid calling setState() directly within an effect
  19 |
  20 |  async function addToPlan(item:Recommendation){if(!userId)return;await supabase.from('study_plan_items').upsert({user_id:userId,lesson_id:item.id,reason:item.reason,priority:item.accuracy===null?3:item.accuracy<40?5:item.accuracy<65?4:3,status:'pending'},{onConflict:'user_id,lesson_id'});setPlan(current=>[...new Set([...current,item.id])]);}
  21 |  if(loading)return <main className="grid min-h-screen place-items-center bg-slate-950 text-white">Construyendo tu ruta...</main>;  react-hooks/set-state-in-effect

✖ 9 problems (9 errors, 0 warnings)

