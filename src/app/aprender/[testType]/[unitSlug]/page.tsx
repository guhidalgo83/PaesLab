"use client";
import {useCallback,useEffect,useMemo,useState} from "react";import Link from "next/link";import {useParams} from "next/navigation";import {createClient} from "@/lib/supabase/client";import LearningHeader from "@/components/learning/LearningHeader";import ProgressBar from "@/components/learning/ProgressBar";import type{LearningUnit,Lesson,LessonProgress}from"@/types/learning";
export default function UnitPage(){const params=useParams();const supabase=useMemo(()=>createClient(),[]);const[unit,setUnit]=useState<LearningUnit|null>(null);const[lessons,setLessons]=useState<Lesson[]>([]);const[progress,setProgress]=useState<LessonProgress[]>([]);const[loading,setLoading]=useState(true);
const load=useCallback(async()=>{const testType=String(params.testType??"m1").toUpperCase();const unitSlug=String(params.unitSlug??"");const{data:unitData}=await supabase.from("learning_units").select("*").eq("test_type",testType).eq("slug",unitSlug).eq("is_published",true).maybeSingle();if(!unitData){setLoading(false);return;}setUnit(unitData as LearningUnit);const{data:lessonData}=await supabase.from("lessons").select("*").eq("unit_id",unitData.id).eq("is_published",true).order("sort_order");setLessons((lessonData??[])as Lesson[]);const{data:{user}}=await supabase.auth.getUser();if(user){const{data:p}=await supabase.from("lesson_progress").select("lesson_id,status,progress_percent,best_quiz_score,last_block_order").eq("user_id",user.id);setProgress((p??[])as LessonProgress[]);}setLoading(false);},[params,supabase]);useEffect(()=>{const id=window.setTimeout(()=>{void load();},0);return()=>window.clearTimeout(id);},[load]);
if(loading)return <main className="grid min-h-screen place-items-center bg-slate-950 text-white">Cargando unidad...</main>;if(!unit)return <main className="grid min-h-screen place-items-center bg-slate-950 text-white"><h1 className="text-3xl font-black">Unidad no encontrada</h1></main>;const completed=lessons.filter(l=>progress.some(p=>p.lesson_id===l.id&&p.status==="completed")).length;const pct=lessons.length?Math.round(completed/lessons.length*100):0;const label=unit.test_type==="SCHOOL"?"Matemática escolar":`Matemática ${unit.test_type}`;
return <main className="min-h-screen bg-slate-950 px-5 py-8 text-white"><div className="mx-auto max-w-5xl"><LearningHeader title={unit.title} subtitle={`${label} · ${unit.axis}`}/><section className="py-10"><Link href="/aprender" className="text-sm font-bold text-slate-400">← Todas las unidades</Link><div className="mt-6 rounded-3xl border border-teal-300/20 bg-teal-300/[0.06] p-7"><h1 className="text-4xl font-black">{unit.title}</h1><p className="mt-3 max-w-3xl leading-7 text-slate-400">{unit.description}</p><div className="mt-6"><ProgressBar value={pct} label={`${completed} de ${lessons.length} lecciones`}/></div></div><div className="mt-8 space-y-4">{lessons.map((lesson,index)=>{const done=progress.some(p=>p.lesson_id===lesson.id&&p.status==="completed");return <Link key={lesson.id} href={`/aprender/leccion/${lesson.slug}`} className="flex flex-col gap-4 rounded-2xl border border-white/10 bg-white/[0.04] p-6 hover:border-teal-300 sm:flex-row sm:items-center"><span className={`grid h-12 w-12 flex-none place-items-center rounded-xl font-black ${done?"bg-emerald-300 text-slate-950":"bg-white/10 text-slate-300"}`}>{done?"✓":index+1}</span><div className="flex-1"><p className="text-xs font-bold text-indigo-300">{lesson.difficulty} · {lesson.estimated_minutes} min</p><h2 className="mt-2 text-xl font-black">{lesson.title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{lesson.summary}</p></div><span className="font-bold text-teal-300">{done?"Repasar":"Comenzar"} →</span></Link>})}</div></section></div></main>;
}
